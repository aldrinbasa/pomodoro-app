import "./Timer.css";
import Container from "../UI/Container/Container";

const Timer = () => {
    return (
        <div>
        <Container classes="container-timer">
        </Container>
        </div>
    );
}

export default Timer;